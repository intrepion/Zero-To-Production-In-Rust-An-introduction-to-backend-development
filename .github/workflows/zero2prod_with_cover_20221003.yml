name: zero2prod_with_cover_20221003

on:
  push:
    branches: [ "zero2prod_with_cover_20221003" ]
  pull_request:
    branches: [ "zero2prod_with_cover_20221003" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
